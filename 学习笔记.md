# 电动车 Agent 项目 · 学习笔记（Day 1-5）

## 一、项目架构回顾

```
core/            ← Agent 引擎（手写核心）
  tool_registry.py   工具注册中心：存工具、给LLM看菜单、替LLM执行
  state.py           单次诊断的"病历"：结构化记录
  react_loop.py      Agent心脏：while循环 + LLM调用 + 调工具
  memory.py          记忆：ShortTermMemory + LongTermMemory

tools/           ← Agent 可调用的工具
  hardware_tools.py    get_battery_data（读电压）
  knowledge_tools.py   search_repair_knowledge（搜知识库）
  memory_tools.py      search_history_cases（查历史案例）

main_agent.py     Agent模式入口（LLM自主决定调工具）⭐核心
diagnose_agent.py 流水线模式（固定搜→答，旧版）
config.py         配置集中管理
utils/logger.py   统一日志
test_cases/       测试
```

---

## 二、Day 1：配置集中管理（config.py）

### Q1：为什么要建 config.py？
**答**：配置集中管理，改一处全项目生效。避免路径/参数散落在多个文件，换电脑漏改一个就炸。

### Q2：`PROJECT_ROOT = Path(__file__).parent` 什么意思？
**答**：`__file__` 是当前文件，`.parent` 是它所在文件夹。config.py 在根目录，所以 parent = 根目录。这样路径自动算出来，不用写死 `D:\...`。

### Q3：`model or config.embedding.model` 里 `or` 干什么？
**答**：取第一个"真值"。传了 model 就用传的；没传（None）就用 config 默认值。**优先级：传了用你的，没传用默认。**

### 关键知识点：dataclass
`@dataclass` 自动生成 `__init__`，字段默认值集中定义。取值用 `config.llm.model`（两层 `.`：第一层哪个类，第二层哪个字段）。

---

## 三、Day 2：日志体系（utils/logger.py）

### Q4：`logger.info()` vs `logger.error()` 区别？为什么结果用 print？
**答**：info 是常规信息，error 是错误（必须关注）。**print 给用户看，logger 给开发者看**——两个受众。用户要诊断结果，开发者要内部状态追踪。

### Q5：`get_logger(__name__)` 里 `__name__` 是什么？
**答**：Python 内置变量，表示当前模块名：
- 直接运行 `python xxx.py` → `__name__ = "__main__"`
- 被 `import xxx` → `__name__ = "xxx"`

所以 `if __name__ == "__main__":` = "只有直接运行时才执行下面代码，被 import 就跳过"。

### 日志分级
```
DEBUG < INFO < WARNING < ERROR
```
级别越高越重要。调低级别（DEBUG）能看到调试细节，调高（ERROR）只看错误。

---

## 四、Day 3：异常恢复（@retry）

### Q6：`@retry(stop=stop_after_attempt(3), wait=wait_fixed(2))` 干什么？
**答**：装饰器，给函数套"自动重试"壳：
- 函数**抛异常**时触发（日志打 error 不算失败）
- 最多试 3 次，每次间隔 2 秒
- `reraise=True`：全失败后抛原始错误

类比：打电话没人接 → 等 2 秒重拨 → 3 次都失败才放弃。

### Q7：为什么监控工具耗时？
**答**：`elapsed = time.time() - start`，超过 5 秒打 warning。正常 0.5 秒，异常 15 秒能及时发现是 API 慢而不是代码 bug。

### 关键：@retry 只对"抛异常"生效
`raise ConnectionError(...)` 才触发重试。print/logger 输出不影响程序流程。

---

## 五、Day 4：长期记忆（LongTermMemory）

### Q8：闭包是什么？
**答**：内层函数捕获外层函数的局部变量。`create_memory_tools(memory)` 里定义的 `search_history_cases` 记住了外层的 `memory`，即使外层函数已返回，`memory` 还活着。

```python
def create_memory_tools(memory):
    def search_history_cases(query):  # 闭包：捕获 memory
        return memory.search(query)
```

### Q9：ShortTermMemory vs LongTermMemory

| | ShortTermMemory | LongTermMemory |
|---|---|---|
| 存什么 | 最近 N 轮对话 | 历史维修案例 |
| 存在哪 | 内存 deque | Chroma 磁盘 long_term_db |
| 什么时候丢 | 程序退出就没 | 持久化，重启还在 |
| 检索方式 | 顺序拼接 | 语义搜索（向量相似度） |

### 关键：语义搜索的价值
搜"续航变短"能命中"电池掉电快"——字面不同但意思相近，向量距离近。关键词匹配做不到。

---

## 六、Day 5：异常边界测试

### 测试了 4 个坏情况

| 测试 | 验证 |
|---|---|
| `test_execute_unknown_tool` | 调不存在的工具 → 返回 error 不崩 |
| `test_execute_tool_exception` | 工具内部抛异常 → 捕获返回 error |
| `test_agent_max_iter_protection` | max_iter=0 → 直接超时，不死循环 |
| `test_agent_llm_empty_content` | LLM 返回空 → 正常结束不崩 |

### 修复的真实 bug
`_call_llm` 里每次都 `self.llm.bind_tools(...)`——重复绑定。改为在 `__init__` 里绑一次。

---

## 七、需要补的核心概念

### 1. `bind_tools` 是什么（LangChain 提供）
不是 Python 自带，是 LangChain 的 `ChatTongyi` 类方法。作用：告诉 LLM "你有这些工具可用"。

**关键坑**：`bind_tools` 返回**新的 LLM 对象**，不修改原对象。必须用返回值，否则 `tool_calls` 永远 None。

```python
llm = ChatTongyi(...)              # 原版，只会聊天
llm_with_tools = llm.bind_tools(...)  # 新版，知道有工具
```

### 2. mock（假对象）
测试 Agent 不用真 LLM，造 `FakeLLM` 类替代。核心原理：

```
真实:  llm.invoke(msg) → AIMessage(有 .content 和 .tool_calls)
假的:  FakeLLM.invoke(msg) → FakeResponse(也有 .content 和 .tool_calls)
```

只要假对象提供的属性和真实对象一样，代码就能无感运行。

FakeResponse 为什么要单独一个类？因为 `react_loop.py` 里这样用返回值：

```python
response = self.llm.invoke(messages)   # 拿到 response
ai_text = response.content            # 必须有 .content
tool_calls = response.tool_calls      # 必须有 .tool_calls
```

所以 `invoke()` 返回的对象必须有这两个属性。真实 LLM 返回 AIMessage（自带），假 LLM 返回 FakeResponse（手动提供）。

**写法一：嵌套类（不方便读）**
```python
class FakeLLM:
    def invoke(self, messages):
        class FakeResponse:
            content = ""
            tool_calls = None
        return FakeResponse()
```

**写法二：平级（更清晰，推荐）**
```python
class FakeResponse:
    content = ""
    tool_calls = None

class FakeLLM:
    def bind_tools(self, schemas):
        return self  # 返回自己，保持链式调用

    def invoke(self, messages):
        return FakeResponse()
```

注意：`FakeLLM` 需要 `bind_tools` 方法，因为 `react_loop.py` 里 `self.llm.bind_tools(...)` 会调用它。

### 3. 方法归属怎么判断
- `"abc".upper()` → Python 内置 str
- `llm.invoke()` / `llm.bind_tools()` → LangChain 框架
- `registry.execute()` → 你自己写的

看 import 来源就知道。

### 4. 装饰器 `@`
放在函数上面的 `@xxx`，给函数"套壳"。`@retry` = 给函数加自动重试能力。

### 5. `global` 关键字
函数内部想**修改**外层变量，必须声明 `global`，否则 Python 当它是新的局部变量。

---

## 八、面试高频考点速记

| 考点 | 一句话答案 |
|---|---|
| `__name__` | 直接运行是 `__main__`，被 import 是模块名 |
| 闭包 | 内层函数捕获外层函数的变量 |
| `or` 短路 | 传了用你的，没传用默认 |
| @retry | 抛异常才重试，最多 N 次隔 M 秒 |
| bind_tools | 告诉 LLM 有工具可用，返回新对象 |
| mock 假对象 | 用同属性的假对象替代真对象，测试不花钱不联网 |
| 优雅降级 | 异常被捕获返回 error，不崩 |
| 死循环保护 | max_iter 是循环上限，到点强制退出 |

---

## 九、第 2 周：fixture 化 + 边界测试 + 评测指标 + git

### 9.1 fixture 化（把重复的初始化提出来）
- **fixture** = 测试前的"准备工作"（初始化 LLM、注册工具），pytest 自动准备、自动传进测试函数。
- **scope 选错会串味**：
  - `scope="module"`：一个文件建一次、全测试复用 → 适合无状态的（LLM、registry）。
  - `scope="function"`（默认）：每个测试新建 → 适合有状态的（Agent，有短期记忆）。
  - 判断口诀：**会记东西的用 function，无状态的用 module**。
- **fixture 依赖**：`def agent(llm, registry)` 的参数名就是另一个 fixture 名，pytest 靠名字匹配，不靠位置。
- `conftest.py`：pytest 自动加载的共享配置，放跨文件复用的 fixture。

### 9.2 边界测试（专测输入的两端极端情况）
- 覆盖：工具返回错误 / 工具抛异常 / `max_iter=0` 死循环保护 / LLM 空内容 / 超长输入。
- **连带 bug 教训**：重构后没跑测试 → FakeLLM 缺 `bind_tools`（因为 bind_tools 挪进 __init__ 了）。
- **mock 假对象要模仿真对象的"全部接口"**：真对象多了个方法，假对象就得补。

### 9.3 评测指标 + tool_trace（Day 5）
- **tool_trace**：Agent 在 state 里记录工具调用轨迹（工具名/耗时/是否成功）——可观测性的第一步，所有指标的地基。
- `evaluation/metrics.py` 三个纯函数：`tool_success_rate`（成功率）、`detect_hallucination`（幻觉检测）、`hallucination_rate`（幻觉率）。
- **纯函数**（输入→输出、无副作用）能单独单元测试，不花钱、秒跑——这是"自动化评测"能成立的关键。
- **`re.IGNORECASE` 大坑**：它只让"匹配"忽略大小写，但 `findall` 返回的还是原文，要比较必须自己 `.lower()`，否则 `"Cell3" ≠ "cell3"` 误判成幻觉。
- **关键词断言天生脆**：LLM 会换说法（"电压偏低" vs "远低于"），死板匹配注定漏——这是第 4-6 周换 LLM-judge 的原因。

### 9.4 git 初始化 + 依赖管理
- **三个区域**：工作区（改文件）→ 暂存区（`git add`）→ 版本库（`git commit`，按快门）。
- `.gitignore` 要在 `add` 前写好；`add` 和 `commit` 是两步。
- **commit 消息规范**：`chore`(初始化) `feat`(新功能) `fix`(修bug) `test`(测试) `docs`(文档)。
- **依赖管理**：删掉 `requirements.txt`，`pyproject.toml` 是唯一依赖来源（PEP 621）。装依赖用 `pip install -e .`。

---

## 十、第 3 周：FastAPI 服务化 + 接口测试 + CI

### 9.1 服务化是什么
把 Agent 包成 HTTP 接口，别人（前端 / 其他服务）用 HTTP 就能调用诊断能力，不用 import 你的 Python 代码。
- `api.py`：`POST /diagnose` 收故障描述 → 返回诊断结果 + state 摘要；`GET /health` 健康检查。

### 9.2 Pydantic 校验（用类定义数据结构）
- `BaseModel` + `Field` 定义"入参/出参长什么样"，FastAPI 自动校验（空/超长/缺字段/类型错 → 422），不用手写 if。
- `Field(..., min_length=1)` 里的 `...`（三个点）= Python 的 Ellipsis 对象，作用"必填、没有默认值"，给后面的校验规则让位。
- `response_model=DiagnoseResponse` 的作用：①校验返回数据 ②过滤多余字段 ③生成文档。核心是"接口契约"——白纸黑字写清返回什么。

### 9.3 关键坑点：def vs async def
- Agent 是同步的（`invoke` 阻塞等 LLM），端点用 `def`：FastAPI 丢线程池跑，不卡事件循环。
- 用 `async def` 反而会阻塞整个服务。
- 类比：`def` = 服务员把菜单交给后厨、自己去接待下一位；`async def` 里调同步函数 = 服务员站后厨门口等菜，前台没人了。

### 9.4 关键坑点：惰性初始化（反模式：import 时重初始化）
- 原来 `agent = build_agent()` 在 import 时就执行，而 ChatTongyi 没 key 就崩 → CI 里没 key，一 import api 就炸（连 422 测试都跑不了）。
- 修复两处：
  1. `agent = build_agent()` → `agent = None`（import 时不建）；
  2. 端点里 `global agent` + `if agent is None: agent = build_agent()`（第一次请求才建一次，之后复用）。
- `global`：函数里给变量赋值默认是"新建局部变量"，要改模块级变量必须声明 `global`。
- `if agent is None` 的另一个好处：mock 的 FakeAgent 能直接顶上去用（不是 None 就跳过 build）。

### 9.5 接口测试：三类情况
- 正常(200) / 边界校验(422) / 异常(500) —— 对应**等价类划分 + 边界值法**。
- **mock 的层次**：Week 2 测 AgentLoop 内部，mock 的是 LLM；现在测 API，mock 的是 Agent（FakeAgent 模仿 `run()` 返回 `(answer, state)`，state 必须有 endpoint 读的那 4 个 key）。
- **monkeypatch**：pytest 内置 fixture，`monkeypatch.setattr(api, "agent", FakeAgent())` = 临时替换、测试结束自动还原（不污染其他测试）。
- **不调 LLM 结果会不会不准？** —— 分层：mock 测"管道"（接口通不通），真 LLM 测"结果"（诊断准不准），两者分开。mock 的假结果"不准"没关系，因为它根本不测准不准。
- **TestClient vs requests**：TestClient 进程内模拟（不起服务器、快、适合 pytest/CI）；requests 真起服务器走真实 HTTP（慢、适合对真实环境的冒烟测试）。

### 9.6 git + CI
- **commit = 存本地，push = 传云端**。我（Claude）能帮你 commit，但 push 要你的 GitHub 登录，只能你自己做 → 分工：我 commit 你 push。
- **CI（GitHub Actions）**：每次 push 自动在云端干净机器跑全套测试，防止"改 A 改坏 B"。
- `pip install -e .` 失败的坑：flat layout（多个包堆根目录）让 setuptools 自动发现罢工 → 加 `[tool.setuptools.packages.find]` 显式声明包。

### 9.7 冒烟测试 + OpenAPI
- 冒烟测试 = 部署后快速验证"服务活着、核心接口能通"（点火试发动机）。
- **冒烟测试必须服务器开着**（requests 要连真服务器），退出就 Connection refused。
- OpenAPI：`/docs` 是漂亮网页，`/openapi.json` 是原始 JSON（`paths` = 接口，`components.schemas` = 数据模型）。写 BaseModel 的过程就是在"顺便写文档"。

### 9.8 我的疑问汇总（Q&A）
| 疑问 | 答案 |
|---|---|
| keywords 和 hits 一样吗？ | 不一定。keywords 是"检查清单"（输入），hits 是"实际命中的"（输出），hits 是 keywords 的子集 |
| 不调 LLM 结果不准怎么办？ | 分层测试：mock 测管道，真 LLM 测结果，两者分开 |
| monkeypatch 是什么？ | pytest 内置工具，临时替换属性、用完自动还原（避免串味） |
| 为什么修 api.py 那两处？ | 把"建 Agent"从 import 时刻挪到第一次请求时刻（惰性初始化），没 key 也不崩 |
| 冒烟测试要退出服务器吗？ | 反了，必须开着服务器（requests 要连它） |
| 怎么确认冒烟测试过了？ | 看服务器日志：/health→200、/diagnose→422，正好匹配 assert |
